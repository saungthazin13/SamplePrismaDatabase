<!DOCTYPE html>
<html>
  <head>
    <%- include("includes/head") %>
  </head>
  <body>
    <%- include("includes/nav") %>
    <h1><%= title %></h1>

    <!-- select for text for each  -->
    <ul>
      <% users.forEach(user => { %>
      <li>User Name: <%= user.name %>, Age: <%= user.age %></li>
      <% }) %>
    </ul>
  </body>
</html>
